import React, {useState} from "react";
import "../App.css"

const Snake = ({Arr}) => {
    return <div>
        <div className="board" style={{ backgroundColor: "green" }} />
    </div>
}
export default Snake;